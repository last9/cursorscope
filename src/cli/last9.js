/** Last9 UI: OpenTelemetry integration (copy OTLP endpoint + auth). */
export const LAST9_OTEL_INTEGRATION_URL =
  "https://app.last9.io/integrations?category=all&integration=OpenTelemetry";

/** Shown during setup — OTLP credentials are admin-only in Last9. */
export const LAST9_OTEL_ADMIN_NOTE =
  "The OTLP auth token is only visible to Last9 organization admins. " +
  "If you are not an admin, ask your Last9 admin to copy it from the integration page and share it with you.";
